{"input":"trait Draw { fn draw(&self) -> i32; }\nstruct C;\nimpl Draw for C { fn draw(&self) -> i32 { 1 } }\nfn run<T: Draw>(x: &T) -> i32 { x.draw() }\nfn main() { let c = C; let _ = run(&c); }","output":"trait Draw { fn draw(&self) -> i32; }\nstruct C;\nimpl Draw for C { fn draw(&self) -> i32 { 1 } }\nfn run(x: &dyn Draw) -> i32 { x.draw() }\nfn main() { let c = C; let _ = run(&c); }"}
{"input":"trait Task { fn step(&self) -> i32; }\nstruct T;\nimpl Task for T { fn step(&self) -> i32 { 2 } }\nfn exec<X: Task>(x: X) -> i32 { x.step() }\nfn main() { let _ = exec(T); }","output":"trait Task { fn step(&self) -> i32; }\nstruct T;\nimpl Task for T { fn step(&self) -> i32 { 2 } }\nfn exec(x: Box<dyn Task>) -> i32 { x.step() }\nfn main() { let _ = exec(Box::new(T)); }"}
{"input":"trait Work { fn work(&self) -> i32; }\nstruct A; struct B;\nimpl Work for A { fn work(&self) -> i32 { 1 } }\nimpl Work for B { fn work(&self) -> i32 { 2 } }\nfn sum<W: Work>(items: Vec<W>) -> i32 { items.iter().map(|x| x.work()).sum() }\nfn main() {}","output":"trait Work { fn work(&self) -> i32; }\nstruct A; struct B;\nimpl Work for A { fn work(&self) -> i32 { 1 } }\nimpl Work for B { fn work(&self) -> i32 { 2 } }\nfn sum(items: Vec<Box<dyn Work>>) -> i32 { items.iter().map(|x| x.work()).sum() }\nfn main() { let _ = sum(vec![Box::new(A), Box::new(B)]); }"}
